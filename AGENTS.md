# AGENTS.md

## 项目目标

Novel Memory Engine 是一个本地优先的长篇小说大纲管理软件。开发时应围绕作者的结构化管理需求展开，包括项目、章节、摘要、大纲、角色状态、剧情线、伏笔和一致性问题。

## 当前阶段

当前阶段只搭建基础工程结构和最小可运行版本。

不要在本阶段实现：

- AI 续写
- AI 摘要调用
- 云同步
- 登录
- 支付
- 多人协作

## 技术约束

- 前端使用 React + TypeScript。
- 桌面端使用 Tauri。
- 本地数据库使用 SQLite。
- 数据访问应通过清晰的 Rust/Tauri 命令或后续抽象层完成，不要让 UI 直接依赖零散 SQL 字符串。
- 数据模型变更应通过 `src-tauri/migrations/` 下的 SQL 迁移维护。

## 代码组织规则

- `src/types/` 放领域类型。
- `src/data/` 只放临时 mock 或种子数据，真实数据接入后应减少使用。
- `src-tauri/migrations/` 放 SQLite schema 和迁移。
- UI 组件应按功能拆分，避免把复杂业务逻辑堆在 `App.tsx`。
- 与 AI 相关的代码后续应独立放在明确模块中，不要混入章节 CRUD 或数据库基础设施。

## 产品边界

本软件帮助作者记忆、整理和检查长篇小说结构，不替作者写正文。任何后续 AI 能力都应服务于摘要、提取、对照、提醒和检查，而不是默认生成小说正文。

## 开发建议

- 优先实现本地项目 CRUD。
- 然后实现文本导入和自动分章。
- 再实现章节摘要和总大纲的人工编辑版本。
- 最后再考虑接入 AI 摘要、角色状态抽取和剧情偏移检测。

## 验证要求

提交变更前至少确认：

- `npm install` 可以安装依赖。
- `npm run build` 可以通过。
- 安装 Rust 和 Tauri 依赖后，`npm run tauri:dev` 可以启动桌面应用。
